<div class="p-5 h-full w-full">
    <div class="bg-white h-full w-full rounded-3xl drop-shadow-2xl py-10 px-5">
            <h1 class="text-center mb-2">
                <%= "<--    #{Date.today}    -->"%>
            </h1>
            <hr class="rounded border-2 border-red-400">

            <%= render(TaskList::ElementComponent.new) %>
            <%= render(TaskList::ElementComponent.new) %>
            <%= render(TaskList::ElementComponent.new) %>
            <%= render(TaskList::ElementComponent.new) %>
            <%= render(TaskList::ElementComponent.new) %>
            <%= render(TaskList::ElementComponent.new) %>
            <%= render(TaskList::ElementComponent.new) %>
    </div>
</div>
